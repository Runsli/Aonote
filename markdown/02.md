---
title: Markdown示例
date: 2026-01-10
summary: 主要介绍Markdown的一些用法
tags: [basic]
---

这个页面用来覆盖常见 Markdown 和 no-JS HTML 样式。顺序从基础排版开始，逐步过渡到列表、提示、媒体、表格、代码和高级组件。

## 标题

<!-- markdownlint-disable -->

# 一级标题

<!-- markdownlint-restore -->

## 二级标题

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

```md
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

## 段落与换行

这是一个段落。

这是另一个段落。

这是一句话不过我要在这里  
换行

::: tip
上方的代码中 `这里` 后面有两个空格。
:::

```md
这是一个段落。

这是另一个段落。

这是一句话不过我要在这里  
换行
```

## 行内文本

这句话里拥有**加粗**、*倾斜*、~~删除~~、`行内代码`、<mark>重点标记</mark> 和快捷键 <kbd>⌘</kbd> + <kbd>K</kbd>。

经典 Emoji: :wink: :cry: :laughing: :yum:

简写 Emoji: 8-) :) :\* :( :-) :-( ;)

```md
这句话里拥有**加粗**、*倾斜*、~~删除~~、`行内代码`、<mark>重点标记</mark> 和快捷键 <kbd>⌘</kbd> + <kbd>K</kbd>。
```

## 链接

[根目录访问主页](/)

[站内文章示例](/posts/01/)

[根目录访问关于](/about)

[归档页面](/archive/)

```md
[根目录访问主页](/)
[站内文章示例](/posts/01/)
[根目录访问关于](/about)
[归档页面](/archive/)
```

## 分割线

---

```md
---
```

## 数学公式

行内公式示例：$E = mc^2$。

块级公式示例：

$$
\int_0^1 x^2 dx = \frac{1}{3}
$$

````md
行内公式示例：$E = mc^2$。

块级公式示例：

$$
\int_0^1 x^2 dx = \frac{1}{3}
$$
````

## 列表

### 无序列表

- 无序列表项
- 无序列表项
  - 列表中的列表项
    - 更多的列表项
    - 更多的列表项
    - 更多的列表项
  - 列表中的长列表项，这个列表项很长。

    而且由很多个段落构成。

    甚至最后一个段落还包含了[链接](#链接)。
- 无序列表项

```md
- 无序列表项
- 无序列表项
  - 列表中的列表项
    - 更多的列表项
  - 列表中的长列表项，这个列表项很长。

    而且由很多个段落构成。

    甚至最后一个段落还包含了[链接](#链接)。
```

### 有序列表

1. 有序列表第一项
1. 有序列表第二项  
   第二项的需要换行  
   再次换行
1. 有序列表第三项

::: tip
上方的代码中 `换行` 后面也有两个空格。
:::

```md
1. 有序列表第一项
1. 有序列表第二项  
   第二项的需要换行  
   再次换行
1. 有序列表第三项
```

### 任务列表

- [x] 已完成的任务
- [ ] 未完成的任务
- [ ] 静态页面中复选框不可点击

```md
- [x] 已完成的任务
- [ ] 未完成的任务
- [ ] 静态页面中复选框不可点击
```

### 定义列表

静态站点
: 只由 HTML、CSS 和静态资源组成，不依赖客户端 JavaScript 运行时。

RSS
: 一种用于订阅网站更新的格式，可以被阅读器聚合。

```md
静态站点
: 只由 HTML、CSS 和静态资源组成，不依赖客户端 JavaScript 运行时。

RSS
: 一种用于订阅网站更新的格式，可以被阅读器聚合。
```

## 引用

> 引用也可以连用
>
> > 可以添加额外的大于号制造更深的引用

```md
> 引用也可以连用
>
> > 可以添加额外的大于号制造更深的引用
```

### 带来源的引用

> 简洁并不意味着缺少结构，而是每个结构都有清晰的用途。
>
> <cite>PureMo-Blog 阅读样式测试</cite>

```md
> 简洁并不意味着缺少结构，而是每个结构都有清晰的用途。
>
> <cite>PureMo-Blog 阅读样式测试</cite>
```

## 提示块

::: tip 提示
这是 VuePress/VitePress 风格的 `tip` 提示块。
:::

!!! note "说明"
    这是 Python-Markdown 原生的 `note` 提示块。

!!! warning "注意"
    这里适合放置需要读者留意的信息。

!!! danger "危险"
    这里适合放置破坏性操作或重要风险提醒。

````md
::: tip 提示
这是 VuePress/VitePress 风格的 `tip` 提示块。
:::

!!! note "说明"
    这是 Python-Markdown 原生的 `note` 提示块。

!!! warning "注意"
    这里适合放置需要读者留意的信息。

!!! danger "危险"
    这里适合放置破坏性操作或重要风险提醒。
````

## 图片

```md
![Logo](/logo.png)
```

### 图片说明

![示例图片](/static/example-image.svg)
*这是图片说明文字，用于测试 caption 样式。*

## 表格

|     居中      |         右对齐 | 左对齐         |
| :-----------: | -------------: | :------------- |
| 居中使用`:-:` | 右对齐使用`-:` | 左对齐使用`:-` |
|       b       |      aaaaaaaaa | aaaa           |
|       c       |           aaaa | a              |

```md
|     居中      |         右对齐 | 左对齐         |
| :-----------: | -------------: | :------------- |
| 居中使用`:-:` | 右对齐使用`-:` | 左对齐使用`:-` |
|       b       |      aaaaaaaaa | aaaa           |
|       c       |           aaaa | a              |
```

### 宽表格

| 日期 | 标题 | 分类 | 标签 | 状态 | 链接 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-05-17 | Markdown 示例页扩展 | 文档 | markdown, no-js, style | 已发布 | https://example.com/posts/markdown-style-example |
| 2026-05-18 | 纯 CSS 组件测试 | UI | css, html, static | 草稿 | https://example.com/posts/css-only-components |

## 代码

行内代码效果: `code`

```md
行内代码效果: `code`
```

### 普通代码块

```md
Sample text here...
```

### 带标题的代码块

```python title="hello.py"
def greet(name):
    return f"Hello, {name}!"
```

````md
```python title="hello.py"
def greet(name):
    return f"Hello, {name}!"
```
````

### 带行高亮的代码块

```python title="highlight.py" hl_lines="2 4-5"
def greet(name):
    message = f"Hello, {name}!"
    print(message)
    return message
```

````md
```python title="highlight.py" hl_lines="2 4-5"
def greet(name):
    message = f"Hello, {name}!"
    print(message)
    return message
```
````

### Diff 加减高亮

```diff title="example.patch"
 function greet(name) {
-  return "Hello, " + name;
+  return `Hello, ${name}!`;
 }
```

`````md
```diff title="example.patch"
 function greet(name) {
-  return "Hello, " + name;
+  return `Hello, ${name}!`;
 }
```
`````

### 缩进代码

```
// Some comments
line 1 of code
line 2 of code
line 3 of code
```

````md
    // Some comments
    line 1 of code
    line 2 of code
    line 3 of code
````

### 嵌套代码围栏

````md
块级代码

```
Sample text here...
```
````

### 长代码行

```bash
curl "https://example.com/api/articles?category=markdown&tag=no-js&include=metadata,summary,content,links,license&sort=published_at_desc"
```

## 原生折叠块

<details>
<summary>展开查看更多</summary>

这里是使用原生 HTML 的折叠内容，不需要 JavaScript。

</details>

```html
<details>
<summary>展开查看更多</summary>

这里是使用原生 HTML 的折叠内容，不需要 JavaScript。

</details>
```

## 脚注

这里有一个脚注引用。[^markdown-footnote]

[^markdown-footnote]: 这是脚注内容，可以放补充说明、来源或延伸阅读。

```md
这里有一个脚注引用。[^markdown-footnote]

[^markdown-footnote]: 这是脚注内容，可以放补充说明、来源或延伸阅读。
```

## 混排段落

这是一段中文、English、数字 123、URL https://example.com/path/to/resource 和行内代码 `inline_code()` 混排的测试文本，用来观察行高、换行和标点间距。